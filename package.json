{
  "name": "moviefinder.app",
  "version": "0.0.1",
  "private": true,
  "scripts": {
    "build": "bun x shadow-cljs release frontend backend",
    "clean": "bun x shadow-cljs clean",
    "frontend": "bun x shadow-cljs watch frontend",
    "backend": "bun x shadow-cljs watch backend",
    "watch": "bun x concurrently \"npm run frontend\" \"npm run backend\"",
    "start": "bun --watch out/backend/main.js",
    "preview": "npm install && npm run build && npm run start"
  },
  "devDependencies": {
    "shadow-cljs": "2.28.20"
  },
  "dependencies": {
    "@electric-sql/pglite": "^0.2.16",
    "react": "^19.0.0",
    "react-dom": "^19.0.0"
  }
}
